The canonical documentation for this repo is [README.md](README.md). It is organized by task — generating a folie, authoring a spec, extending the grammar, editing the generator. The same sections serve human contributors and AI agents.

Before non-trivial changes, read the relevant session in `test-log.md`. Append to it after.
